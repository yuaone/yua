# YUA 1.0 — Dependencies
# Generated by AST analysis of src/model, src/inference, src/tokenizer
# stdlib modules excluded (json, math, logging, etc.)

# Core
torch>=2.1.0
sentencepiece>=0.2.0
pyyaml>=6.0

# Vision (optional — for VLM)
Pillow>=10.0.0
# transformers>=4.40.0  # uncomment when using SigLIP vision encoder
